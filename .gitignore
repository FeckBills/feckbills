node_modules/
dist/
*.tsbuildinfo
*.log

# Local env / credentials — never commit
.env
.env.*
!.env.example
*.json.key
service-account*.json

# Generated reports
reports/
*.report.md

# Demo source videos — the README embeds the optimised feckbills.gif instead
# (rendered square from apps/video in the platform repo). Keep mp4 sources out of git.
*.mp4

# OS / editor
.DS_Store
.idea/
.vscode/
